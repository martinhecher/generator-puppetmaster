language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-puppetmaster' ]; then cd .. && eval "mv $currentfolder generator-puppetmaster" && cd generator-puppetmaster; fi

